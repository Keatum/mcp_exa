# Example environment configuration for Exa MCP Server

# Copy this file to `.env` and replace the placeholder API key with your own.

# Your Exa API key. Generate one from the Exa dashboard.
EXA_API_KEY=your-exa-api-key

# Optional: the port that the MCP server will bind to. Defaults to 5000 if not set.
EXA_MCP_SERVER_PORT=5000